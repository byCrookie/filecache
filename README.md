# FileCache

A lightweight, single-file SQLite cache implementing .NET’s `IDistributedCache`.

## Usage

Copy [FileCache.cs](FileCache.cs) into your project.
Requires `Microsoft.Data.Sqlite` and `Microsoft.Extensions.Caching.Abstractions`.

Compatible with .NET Standard 2.1+ and C# 9.0+.
